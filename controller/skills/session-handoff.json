{
  "name": "session-handoff",
  "description": "Save snapshot, record progress milestone, and update design intent status. Use at end of session for clean handoff.",
  "commands": [
    "snapshot save handoff-{timestamp}"
  ],
  "params": {
    "milestone": {
      "description": "Milestone name for progress log",
      "default": "session_end"
    },
    "summary": {
      "description": "One-line summary of session work",
      "default": "Session ended, state saved."
    }
  },
  "notes": "After running, also call progress_update and update_intent."
}
