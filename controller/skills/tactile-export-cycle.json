{
  "name": "tactile-export-cycle",
  "description": "Switch to presentation style, render tactile PDF, check quality, then restore working style. Use before printing to PIAF.",
  "commands": [
    "snapshot save pre-export-{timestamp}",
    "style use {style}",
    "export piaf",
    "style use working"
  ],
  "params": {
    "style": {
      "description": "Style profile for export (e.g. presentation, piaf-high-contrast)",
      "default": "presentation"
    }
  }
}
